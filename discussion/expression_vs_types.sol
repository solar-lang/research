

type names get read like this:

    Map A B

    -> Map<A<B>> instead of Map<A, B>

        - in order to write Map<A, B>, one would use Map (A, B)

while expressions get read like this
    a b c

    a(b, c)

    instead of a(b(c))
